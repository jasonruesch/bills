{
  "name": "bills-data-access",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/bills/data-access/src",
  "projectType": "library",
  "targets": {
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["supabase start"],
        "cwd": "libs/bills/data-access"
      }
    },
    "stop": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["supabase stop"],
        "cwd": "libs/bills/data-access"
      }
    },
    "create-local-snapshot": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx ts-node libs/bills/data-access/src/create-snapshot-of-local.ts -P libs/bills/data-access/tsconfig.json"
        ],
        "parallel": false
      }
    },
    "create-prod-snapshot": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx ts-node libs/bills/data-access/src/create-snapshot-of-prod.ts -P libs/bills/data-access/tsconfig.json"
        ],
        "parallel": false
      }
    },
    "restore-snapshot-to-local-db": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx ts-node libs/bills/data-access/src/restore-snapshot.ts -P libs/bills/data-access/tsconfig.json"
        ],
        "parallel": false,
        "__unparsed__": []
      }
    },
    "clear-local-db-data": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "PGPASSWORD=postgres psql -U postgres -h 127.0.0.1 -p 54322 -f libs/bills/data-access/supabase/clear-db.sql"
        ],
        "parallel": false,
        "__unparsed__": []
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  },
  "tags": []
}
